import { PhoneIntlPrexixPipe } from './phone-intl-prexix.pipe';

describe('PhoneIntlPrexixPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneIntlPrexixPipe();
    expect(pipe).toBeTruthy();
  });
});
